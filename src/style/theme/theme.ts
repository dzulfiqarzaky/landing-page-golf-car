import { colors, gradient } from "./colors";
import { mediaQuery } from "./mediaQuery";
import { fontSize, lineHeight, fontFamily } from "./typography";

export const theme = {
  fontSize,
  colors,
  mediaQuery,
  lineHeight,
  fontFamily,
  gradient,
};
